import 'package:flutter/material.dart';
import 'splash_screen.dart';
class Auth extends StatelessWidget {
  const Auth({super.key, Key});

  @override
  Widget build(BuildContext context) {
    return const SplashScreen();
  }
}
